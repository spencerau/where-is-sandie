{
  "name": "where-is-sandie",
  "version": "1.0.0",
  "description": "- Run `pip3 install -r requirements.txt` to install the required packages.  - Run `git submodule update --init --recursive --remote` to install FindMyHistory submodule fork",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node server.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "csv-parser": "^3.0.0",
    "multer": "^1.4.5-lts.1"
  }
}
